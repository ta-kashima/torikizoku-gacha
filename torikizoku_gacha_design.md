# 鳥貴族メニューガチャ 設計書

**バージョン**: v1.7  
**作成日**: 2026-04-30  
**最終更新**: 2026-06-02  
**ステータス**: 実装済み・運用中

---

## 1. 概要

### 1.1 プロジェクト名
鳥貴族メニューガチャ（仮称: TorikizokuGacha）

### 1.2 目的
サイゼリヤメニューガチャ（wxcollector.cloudfree.jp/saizeriya）の鳥貴族版として、鳥貴族のメニューをカスタム条件に従ってランダム表示するファンサイトを構築する。

### 1.3 本家（サイゼリヤ版）との主な差分

| 項目 | サイゼリヤ版 | 鳥貴族版 |
|------|------------|---------|
| ガチャ主軸 | 合計金額（1000円基準） | **品目数**（全品390円均一のため金額軸は補助表示のみ） |
| 栄養素軸 | カロリー上限・塩分上限 | **カロリー + PFC目標値（±許容範囲内でスコア最小を採用）** |
| カテゴリー | ドリンク/サイド/メイン/テイクアウト | 鳥貴族公式メニュー区分に準拠（§2.3参照） |

### 1.4 非公式ファンサイトとしての位置付け
- 鳥貴族公式・株式会社エターナルホスピタリティグループとは無関係である旨をフッターに明記する
- 商標・ロゴ画像は使用せず、テキストベースで構成する

---

## 2. ドメインモデル

### 2.1 メニュー型定義

```typescript
type Menu = {
  id: string;            // 一意ID（例: "yakitori-001"）
  name: string;          // メニュー名（例: "もも貴族焼 タレ"）
  price: number;         // 価格（現在は390で固定。値上げ対応のためフィールドとして保持）
  category: Category;   // 大分類
  type: MenuType;        // 小分類
  calories: number;          // カロリー kcal
  carbs: number | null;      // 炭水化物 g（栄養成分非公開品は null → ガチャ対象外）
  protein: number | null;    // タンパク質 g（同上）
  fat: number | null;        // 脂質 g（同上）
  salt: number;              // 食塩相当量 g
  isAlcohol: boolean;    // アルコールフラグ
  isLimited: boolean;    // 期間限定フラグ
  availableUntil?: string; // 期間限定終了日（ISO 8601 例: "2025-09-30"）
};
```

### 2.2 カテゴリー（大分類）
公式メニューページの区分に準拠。テイクアウトは対象外とし、イートイン品のみを扱う。

| カテゴリーID | 表示名 |
|------------|-------|
| `yakitori` | 焼鳥 |
| `ippin` | 逸品料理 |
| `speed` | スピードメニュー |
| `gohan` | ご飯・デザート |
| `drink` | ドリンク |

### 2.3 タイプ（小分類）

| タイプID | 表示名 | 属するカテゴリー |
|---------|--------|--------------|
| `kizokuyaki` | 貴族焼 | 焼鳥 |
| `teiban_kushi` | 定番串 | 焼鳥 |
| `sosaku_kushi` | 創作串 | 焼鳥 |
| `age` | 揚げ物 | 逸品料理 |
| `teppan` | 鉄板焼 | 逸品料理 |
| `salad` | サラダ | 逸品料理 |
| `ippin_other` | その他逸品 | 逸品料理 |
| `tsumami` | おつまみ | スピードメニュー |
| `reisai` | 冷菜 | スピードメニュー |
| `gohan_mono` | ご飯もの | ご飯もの・デザート |
| `dessert` | デザート | ご飯もの・デザート |
| `beer` | ビール | ドリンク |
| `sour` | サワー | ドリンク |
| `highball` | ハイボール | ドリンク |
| `cocktail` | カクテル | ドリンク |
| `sake_shochu` | 日本酒・焼酎 | ドリンク |
| `non_alcohol` | ノンアル | ドリンク |
| `soft` | ソフトドリンク | ドリンク |

### 2.4 ガチャ設定型定義

```typescript
type GachaConfig = {
  // 主軸
  count: number;                    // 品目数（デフォルト: 5）

  // 重複制御
  allowDuplicateMenu: boolean;      // メニュー重複許可（デフォルト: true）
  allowDuplicateCategory: boolean;  // カテゴリー重複許可（デフォルト: true）
  allowDuplicateType: boolean;      // タイプ重複許可（デフォルト: true）

  // アルコール除外
  excludeAlcohol: boolean;          // アルコール除外（デフォルト: false）

  // 期間限定除外（現在一時停止中。menu_limited.json の読み込みも停止）
  // excludeLimited: boolean;       // 期間限定除外（デフォルト: false）

  // PFC目標値（スコアリング方式）
  pfcGoal: {
    carbs?: number;    // 炭水化物 目標値 g
    protein?: number;  // タンパク質 目標値 g
    fat?: number;      // 脂質 目標値 g
  };

  // PFC許容範囲（目標値に対する割合、デフォルト: 20）
  // 例: 20 → 目標値 ±20% の範囲内を「許容」と判定
  pfcTolerancePct: number;

  // PFCバランスプリセット（pfcGoal の値を自動入力するための補助）
  pfcPreset: 'standard' | 'muscle_gain' | 'fat_loss' | 'lowcarb' | 'custom';

  // カロリー目標値
  caloriesGoal?: number;  // カロリー 目標値 kcal

  // 塩分上限（※塩分のみ上限値として管理。目標値・許容範囲の対象外）
  saltMax?: number;       // 塩分 上限 g（合計がこの値を超えた組み合わせは除外）

  // 固定ピン（ランダム抽選前に確定させる品目）
  pinnedIds: string[];              // ピン留めするメニューIDの配列
  // 例: ["drink-001", "shioyaki-004"]
  // ピン品は count に含まれる。残り（count - pinnedIds.length）枠をランダム抽選する。
  // ピン品の数が count 以上の場合はランダム抽選なし（ピン品のみで結果を返す）

  // 包含・排他
  mustIncludeCategory: Category[];  // 必ず1つ以上含むカテゴリー
  mustIncludeType: MenuType[];      // 必ず1つ以上含むタイプ
  onlyOneCategory: Category[];      // 1つだけ含むカテゴリー
  onlyOneType: MenuType[];          // 1つだけ含むタイプ
  excludeCategory: Category[];      // 除外カテゴリー
  excludeType: MenuType[];          // 除外タイプ
};
```

---

## 3. 機能仕様

### 3.1 ガチャ基本動作
ユーザーが設定した `GachaConfig` に従い、メニュープールからランダムに `count` 品を選択して結果を表示する。

### 3.2 品目数プリセット

| プリセット | 品目数 | 合計金額（参考） |
|---------|------|--------------|
| 3品 | 3 | ¥1,170 |
| 4品 | 4 | ¥1,560 |
| 5品 | 5 | ¥1,950 |
| 7品 | 7 | ¥2,730 |
| 10品 | 10 | ¥3,900 |
| カスタム | 任意入力 | ¥{品目数 × 390}（目安） |

合計金額はあくまで参考値として自動算出・表示するが、ガチャの制約条件には使用しない。  
ガチャ実行前の推定表示は `count × 390`（`ITEM_PRICE`）で算出する。ガチャ結果の合計金額は `items.reduce((s, m) => s + m.price, 0)` で各品目の実価格を合算する。

### 3.3 PFC目標値（スコアリング方式）

#### 3.3.1 基本方針

PFCは「目標値にできるだけ近い組み合わせを優先する」スコアリング方式で実装する。  
目標値に対して **±`pfcTolerancePct`%（デフォルト: ±20%）** の許容範囲を設け、その範囲内で最もスコアが良い組み合わせを採用する。  
許容範囲を超えた組み合わせも「許容範囲内で最良のもの」として表示し、超過・不足をユーザーに明示する。

#### 3.3.2 スコアリング計算

設定した各栄養素について、目標値との相対誤差の合計をスコアとする。スコアが小さいほど目標値に近い。

```
score = Σ |actual_i - goal_i| / goal_i   （設定済みの各栄養素 i について）
```

カロリーも目標値が設定されている場合は同様にスコアに加算する。

#### 3.3.3 採用ロジック

```
【フェーズ1】100,000 回の試行全体を通じて、許容範囲内のスコア最小候補を追跡する
  条件: 全ての設定済み栄養素について
        goal_i × (1 - pfcTolerancePct/100)
        ≤ actual_i ≤
        goal_i × (1 + pfcTolerancePct/100)
  → 試行終了後、許容範囲内の最良候補（bestInRange）が存在すれば採用 ✅

【フェーズ2】許容範囲内の候補が 0 件の場合
  → 同じ 100,000 回の試行中に追跡したスコア最小の候補（bestOverall）を採用
  → 結果画面に「目標値から外れています」と警告表示 ⚠️
```

許容範囲を外れていても必ず何らかの結果を返すため、「見つかりませんでした」にはならない。

#### 3.3.4 PFCバランスプリセット

ユーザーがプリセットを選択すると、目標カロリーを基に各栄養素の目標値を自動入力する（手動上書き可能）。

| プリセット名 | 想定ユーザー | P比率 | F比率 | C比率 | 備考 |
|------------|-----------|------|------|------|------|
| バランス（標準） | 一般 | 15% | 25% | 60% | 厚生労働省 食事摂取基準に準拠 |
| 筋肉増量 | 増量・トレーニング層 | 25% | 25% | 50% | P20〜30%・F20〜30%・C40〜60%の中央値 |
| 体脂肪減少 | 減量層 | 30% | 25% | 45% | P25〜35%・F20〜30%・C35〜50%の中央値 |
| 糖質制限 | ダイエット層 | 30% | 50% | 20% | 脂質多め・糖質抑制 |
| カスタム | 全員 | 任意 | 任意 | 任意 | 直接入力 |

#### 3.3.5 プリセットからPFC目標値の自動算出

目標カロリーが入力されている場合、プリセット選択時に以下の計算で各フィールドへ自動入力する:

```
目標炭水化物 (g) = 目標カロリー × (C比率 / 100) / 4
目標タンパク質 (g) = 目標カロリー × (P比率 / 100) / 4
目標脂質 (g)     = 目標カロリー × (F比率 / 100) / 9
```

自動入力後にユーザーが個別に値を調整した場合はカスタム扱いに切り替わる。

#### 3.3.6 プロフィールから自動計算

ユーザーが身長・体重・年齢・性別・活動量を入力すると、TDEE（1日の総消費カロリー）を算出し、目標カロリー・PFC目標値を自動入力する機能。

**UI方式**: 「PFC目標値」カード内の折りたたみフォーム（デフォルト閉じ）  
プロフィール未入力のユーザーは現行の手動入力・プリセット選択をそのまま利用できる。

**計算式**

BMR（基礎代謝） ── Mifflin-St Jeor式:
```
男性: 10×体重(kg) + 6.25×身長(cm) - 5×年齢(歳) + 5
女性: 10×体重(kg) + 6.25×身長(cm) - 5×年齢(歳) - 161
```

TDEE（1日の総消費カロリー）:
```
TDEE = BMR × 活動係数
  座りがち（ほぼ運動なし） → ×1.2
  軽い運動（週1〜3日）   → ×1.375
  適度な運動（週3〜5日） → ×1.55
  ハードな運動（週6〜7日）→ ×1.725
```

このセッション（トリキでの1食・飲み会）分の目標カロリー:
```
目標カロリー = TDEE × セッション割合
  1食分（33%）
  飲み会分（50%）
  カスタム（任意 %）
```

目標カロリーが決まった後は、既存のプリセット選択でPFC各値を自動入力する（§3.3.5と連動）。

**UIレイアウト（案2：折りたたみフォーム）**

```
▼ プロフィールから計算（任意）
  身長 [___] cm   体重 [___] kg   年齢 [___] 歳
  性別 [男][女]
  活動量 [座りがち][軽い][適度][ハード]
  セッション割合 [1食分(33%)][飲み会(50%)][カスタム ___]%
  ─────────────────────────────────────
  推奨: 約 680 kcal       [目標値に適用]

目標カロリー [680] kcal   ← 適用後に自動入力
プリセット [バランス][筋肉増量][体脂肪減少][糖質制限][カスタム]
```

#### 3.3.7 栄養成分不明品の扱い
- 栄養成分が公式資料に記載のない品目はメニュープールから除外する（ガチャ対象外）
- 結果画面では「一部栄養成分不明品はガチャ対象外です」と注記する

### 3.4 ガチャアルゴリズム

```
0. 固定ピンの処理:
   - pinnedIds に含まれるメニューを確定品として取り出す
   - 有効な残り枠数 = count - pinnedIds.length
   - 残り枠数 ≤ 0 の場合 → ピン品のみで結果を返す（ランダム抽選なし）
   - ピン品が重複制御に違反していても、ピンは優先して確定させる
     （例: カテゴリー重複禁止でも、ユーザーが意図的にピンした品は除外しない）

1. 全メニュープールから以下を除いたサブプールを構築する:
   - 除外カテゴリー / 除外タイプ
   - アルコール除外フラグが ON のアルコール品
   - 期間限定除外フラグが ON の期間限定品（※現在機能一時停止中）
   - 栄養成分が不明（未登録）の品目
   - 既にピン品に含まれている品目（メニュー重複禁止の場合のみ）

2. ランダム選択ループ（最大 100,000 回試行）:
   a. サブプールから残り枠数の品をランダムに選択する（重複不可時はインデックスをSetで管理するリジェクションサンプリング）
   b. ピン品 + ランダム選択品の合計に対して、以下のハードフィルターを検証する:
      - メニュー重複制御（ピン品同士の重複はユーザー責任で許容）
      - カテゴリー重複制御
      - タイプ重複制御
      - 必ず1つ含むカテゴリー / タイプ
      - 1つだけ含むカテゴリー / タイプ
      - 塩分上限（合計 ≤ saltMax）
   c. ハードフィルターを通過した組み合わせについてスコアを計算する:
      score = Σ |actual_i - goal_i| / goal_i  （設定済みの各 PFC + カロリー）
   d. 許容範囲内（全栄養素が goal ± pfcTolerancePct%）の組み合わせを
      bestInRange として保持する（スコアが現 bestInRange より小さければ更新）
   e. 許容範囲外でも bestOverall として保持する（常にスコア最小を保持）

3. ループ終了後の採用ロジック:
   - bestInRange が存在する → bestInRange を結果として採用 ✅
   - bestInRange が存在しない → bestOverall を結果として採用 ⚠️
     → 結果画面に「目標値から外れた栄養素があります」と警告表示

4. PFC目標値が未設定の場合:
   → ハードフィルターを通過した最初の組み合わせを採用（スコアリングなし）

5. サブプールが空 or 全試行でハードフィルターすら通過しない場合:
   → 「条件が厳しすぎます。設定を見直してください」と表示
```

### 3.5 固定ピン機能

#### 3.5.1 概要
「必ずこれは頼む」品目をガチャ前にピン留めし、残り枠だけをランダム抽選する機能。

例: 5品ガチャで「生ビール」をピン → 生ビール確定 + 残り4品をランダム抽選

#### 3.5.2 UI フロー

```
【ピン登録】
  メニュー検索バーで品目名を入力 → 候補がドロップダウン表示（debounce 100ms）
  → 選択すると「📌 ピン済み」リストに追加

【ピン解除】
  ピン済みリストの × ボタンで個別解除
  または「ピンを全解除」ボタンで一括解除

【ガチャ実行】
  ピン品は結果の先頭に表示し、📌 アイコンで識別
```

#### 3.5.3 制約・仕様

| ケース | 挙動 |
|-------|------|
| ピン品数 < count | 残り枠（count - ピン数）をランダム抽選 |
| ピン品数 = count | ランダム抽選なし。ピン品のみで結果を返す |
| ピン品数 > count | ピン品数に合わせて count を自動的に引き上げる（警告表示） |
| ピン品がサブプール除外条件に該当 | ピンを優先し、除外条件より上位として扱う（例: アルコール除外 ON でも、ピン済みのビールは含まれる） |
| ピン品同士でカテゴリー・タイプ重複 | ユーザーの意図として許容する（警告表示のみ） |

#### 3.5.4 PFC計算への影響
ピン品の栄養素はガチャ実行前に合計に加算される。PFC目標値が設定されている場合、ランダム枠のスコアリングは「目標値 − ピン品の合計値」を残り目標値として計算する。

```
残り目標炭水化物 = pfcGoal.carbs - Σ(ピン品の炭水化物)
残り目標タンパク質 = pfcGoal.protein - Σ(ピン品のタンパク質)
残り目標脂質 = pfcGoal.fat - Σ(ピン品の脂質)
```

残り目標値が 0 以下になった場合は、その栄養素のスコアリングをスキップする（ピン品だけで目標を超えているため）。

### 3.6 結果表示

| 表示要素 | 内容 |
|---------|------|
| メニューリスト | 品目名・金額（上段）、カテゴリー・タイプ・PFC・塩分（下段の2段レイアウト） |
| 各メニューのPFC | P ${protein}g　F ${fat}g　C ${carbs}g　塩 ${salt}g（PFCが null の場合は該当項目を省略） |
| 合計サマリー | 品目数、合計金額（各品目の実価格合計）、合計カロリー |
| PFC合計 | 炭水化物 g / タンパク質 g / 脂質 g / 塩分 g |
| PFCバランスグラフ | 横棒グラフ（合計値 vs 目標値、許容範囲を視覚化）✅/⚠️ アイコンで範囲内外を明示 |
| 再ガチャボタン | 同設定で再抽選 |
| シェアボタン | X（Twitter）投稿 / クリップボードコピー |

### 3.7 シェア機能

#### X（Twitter）投稿
Web Intent URL（`https://twitter.com/intent/tweet?text=...`）を使用する。サーバーサイド処理不要で実装可能。

投稿テキストフォーマット:
```
🍗 鳥貴族メニューガチャ結果（5品 / ¥1,950）

1. もも貴族焼 タレ
2. ふんわり山芋鉄板
3. とり釜飯
4. 生ビール
5. 唐揚（3個）

🔥 カロリー: 1,240kcal
💪 P: 72g / F: 48g / C: 85g

#鳥貴族 #トリキガチャ
[サイトURL]
```

#### クリップボードコピー
`navigator.clipboard.writeText()` を使用。上記と同じテキストをコピーする。

---

## 4. 画面設計

### 4.1 レイアウト（1ページ完結）

```
┌─────────────────────────────────────┐
│  🍗 鳥貴族メニューガチャ               │  ← ヘッダー
├─────────────────────────────────────┤
│  [ガチャを回す]  □ アルコールを除く    │  ← CTAエリア
├─────────────────────────────────────┤
│  [ガチャのカスタマイズ ▼]             │  ← アコーディオン（デフォルト閉じ）
│                                      │
│  ■ 固定ピン                          │
│    [🔍 メニューを検索してピン留め   ] │
│    📌 生ビール          × │
│    📌 砂ずり（砂肝）    × │
│    [ピンを全解除]                    │
│    ※ピン品は確定。残り3枠を抽選します│
│                                      │
│  ■ 品目数                            │
│    ○3品  ●5品  ○7品  ○10品  [__]品  │
│    合計金額目安: ¥1,950               │
│                                      │
│  ■ 重複設定                          │
│    メニュー: ●許可 ○禁止             │
│    カテゴリー: ●許可 ○禁止           │
│    タイプ: ●許可 ○禁止               │
│                                      │
│  ■ PFC目標値                         │
│    ▶ プロフィールから計算（任意）     │  ← 折りたたみ
│      身長[___]cm 体重[___]kg 年齢[___]歳│
│      性別 [男][女]                   │
│      活動量 [座りがち][軽い][適度][ハード]│
│      割合 [1食分][飲み会][カスタム__]%│
│      → 推奨: 約680kcal [目標値に適用]│
│                                      │
│    目標カロリー: [____] kcal          │
│                                      │
│    プリセット:                        │
│    [バランス][筋肉増量][体脂肪減少][糖質制限][カスタム] │
│    ↑選択で下記フィールドを自動入力    │
│                                      │
│    炭水化物 [____] g                  │
│    タンパク質[____] g                 │
│    脂質      [____] g                 │
│                                      │
│    許容範囲: ± [20] %                │
│                                      │
│  ■ 塩分上限（任意）                  │
│    塩分: [____] g 以下               │
│                                      │
│  ■ カテゴリー・タイプ指定             │
│    （必ず含む / 1つだけ / 除外）       │
│                                      │
│  [カスタマイズをリセット]              │
├─────────────────────────────────────┤
│  ─── 結果 ───                        │  ← 結果エリア（ガチャ後に表示）
│                                      │
│  📌 生ビール           ¥390           │  ← ピン品（上段: 名前＋金額）
│      ドリンク / ビール                │  ←（下段: タグ＋PFC）
│  📌 砂ずり（砂肝）    ¥390           │
│      焼鳥 / 定番串  P7g F4g C2g 塩0.5g│
│  3. もも貴族焼 たれ   ¥390           │
│      焼鳥 / 貴族焼  P18g F15g C6g 塩1.4g│
│  4. ふんわり山芋鉄板  ¥390           │
│      逸品 / 鉄板焼  P8g F11g C20g 塩1.8g│
│  5. トリキの唐揚      ¥390           │
│      逸品 / 揚げ物  P22g F18g C12g 塩1.2g│
│                                      │
│  品目数: 5品   合計: ¥1,950           │
│  カロリー: 1,240kcal                  │
│  炭水化物: 85g / タンパク質: 72g      │
│  脂質: 48g   / 塩分: 4.2g            │
│                                      │
│  PFC ━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│  炭水化物 [████████░░] 85g / 目標100g│  ✅ 範囲内
│  タンパク質[████████░░] 65g / 目標70g│  ✅ 範囲内
│  脂質      [██████████] 72g / 目標60g│  ⚠️ 目標超過
│                                      │
│  [もう一度ガチャを回す]               │
│  [X（Twitter）にシェア]               │
│  [クリップボードにコピー]              │
└─────────────────────────────────────┘
│  非公式ファンサイト。鳥貴族公式とは無関係│  ← フッター
└─────────────────────────────────────┘
```

### 4.2 レスポンシブ
- モバイルファースト（基準幅: 375px）
- タブレット・PC対応（max-width: 640px でセンタリング）

---

## 5. データ設計

### 5.1 データソース
- **メニュー情報**: 鳥貴族公式グランドメニューPDF（2025年5月〜 390円均一対応版）
- **栄養成分情報**: 鳥貴族公式 アレルギー・栄養成分PDF（2026年4月更新版）
- **収集方法**: 上記公式資料を目視で確認・手動入力

### 5.2 データ形式

`menu.json`（クライアントサイドにバンドル）

```json
[
  {
    "id": "yakitori-001",
    "name": "もも貴族焼 タレ",
    "price": 390,
    "category": "yakitori",
    "type": "kizokuyaki",
    "calories": 228,
    "carbs": 8.2,
    "protein": 18.5,
    "fat": 13.4,
    "salt": 1.2,
    "isAlcohol": false,
    "isLimited": false
  }
]
```

### 5.3 メニューデータ収集方針

以下の単位で収集する（期間限定は別ファイル管理を推奨）:

| ファイル | 内容 |
|--------|------|
| `menu.json` | グランドメニュー（常設品） |
| `menu_limited.json` | 期間限定メニュー（随時更新） |

起動時に両ファイルをマージし、`availableUntil` が今日以降のもののみをプールに含める。  
栄養成分が `null` の品目（ドリンク等で公式資料に記載なし）はガチャプールから除外する。

### 5.4 更新運用
- 値上げ・メニュー改定時: `menu.json` の対象エントリを修正・追加・削除
- 期間限定メニュー追加時: `menu_limited.json` にエントリを追加
- README に更新手順を記載する

---

## 6. 技術スタック

| 層 | 採用技術 | 理由 |
|---|---------|------|
| フレームワーク | **静的 HTML + Vanilla JS**（1ファイル完結）| 学習コスト低・ホスティング容易 |
| スタイル | **カスタム CSS**（CSS カスタムプロパティによるデザイントークン管理） | 外部依存なし・オフライン動作 |
| フォント | **Google Fonts**（Zen Antique, Noto Sans JP）CDN 経由 | 和風デザインとの親和性 |
| グラフ | **カスタム CSS 横棒グラフ** | 外部ライブラリ不要・軽量 |
| データ | `menu.json` / `menu_limited.json`（静的ファイル）| サーバー不要 |
| ホスティング | **GitHub Pages** | 無料・静的サイトに最適 |

> **補足**: 将来的に React + TypeScript + Vite へのリファクタリングも視野に入れ、ビジネスロジック（ガチャアルゴリズム・スコアリング）はモジュールとして分離できる設計にする。

---

## 7. ファイル構成

```
torikizoku-gacha/
├── index.html              # メイン（HTML + CSS + JS を1ファイルに集約）
├── data/
│   ├── menu.json           # グランドメニューデータ（常設品）
│   └── menu_limited.json   # 期間限定メニューデータ
└── torikizoku_gacha_design.md  # 本設計書
```

---

## 8. コードアーキテクチャ

### 8.1 モジュール構成（IIFE パターン）

`index.html` 内の `<script>` はすべて即時実行関数式（IIFE）を用いてグローバル汚染を防ぐ。

| モジュール | 役割 |
|---------|------|
| `DataStore` | メニューデータの fetch・保持・フィルタリング |
| `Gacha` | ガチャアルゴリズム・スコアリング（リジェクションサンプリングによる重複なし抽選） |
| `UIState` | カテゴリー/タイプタグの状態管理（cycleTagState・buildFilters）。`GachaConfig` の組み立ては `getConfigValues()` グローバル関数が担う |
| `PinManager` | ピン品の追加・削除・表示管理 |

### 8.2 主要ユーティリティ

| 関数 | 役割 |
|-----|------|
| `escapeHtml(str)` | DOM への挿入前に `&`, `<`, `>`, `"`, `'` をエスケープ（XSS 防止） |
| `showToast(msg, ms)` | 画面下部トースト通知（`clearTimeout` で二重表示防止） |
| `debounce(fn, wait)` | 入力イベントの間引き（ピン検索: 100ms） |
| `setLoading(visible)` | ローディングオーバーレイの表示切替・ガチャボタンの `disabled` 制御 |
| `runGacha()` | ガチャ実行エントリポイント（`setLoading` で並行実行防止） |

---

## 9. 非機能要件

### 9.1 セキュリティ

| 項目 | 要件・実装 |
|-----|------|
| DOM インジェクション対策 | `innerHTML` への挿入前に `escapeHtml()` で全属性値・テキストをサニタイズ |
| URL エンコード | シェアテキスト生成時に `encodeURIComponent` を使用 |
| データ改ざん防止 | `menu.json` は GitHub リポジトリで管理し、GitHub Pages の読み取り専用配信に委ねる |

### 9.2 パフォーマンス

| 項目 | 要件・実装 |
|-----|------|
| 初回表示 | 2秒以内（低速回線 3G 相当を除く） |
| ガチャ処理 | 最大 100,000 回試行をメインスレッドで同期実行。処理中は `setLoading(true)` でオーバーレイ表示・ボタン無効化 |
| フォント読み込み | Google Fonts を `<link rel="preconnect">` + `display=swap` で非同期読み込み |
| データサイズ | `menu.json` / `menu_limited.json` は単一軽量 JSON を維持。gzip は GitHub Pages のデフォルト機能に委ねる |

> **将来検討**: ガチャ試行回数が増えた場合は Web Worker への切り出しを検討する。

### 9.3 オフライン対応

現バージョン（v1.7）では Service Worker を実装しない。Google Fonts CDN に依存するため完全オフラインには非対応。

> **将来検討**: 居酒屋での利用（不安定な電波）を考慮し、Service Worker によるキャッシュ導入を検討する。

### 9.4 アクセシビリティ

| 項目 | 要件・実装 |
|-----|------|
| 準拠基準 | WCAG 2.1 AA を目安とする |
| カラーコントラスト | テキストのコントラスト比 4.5:1 以上 |
| 色依存の排除 | ✅/⚠️ はアイコン＋テキスト（「範囲内」「目標超過」）を併記し、色だけに依存しない |
| ARIA ランドマーク | アコーディオン本体に `role="region"` / `aria-labelledby` を付与 |
| ボタン状態 | アコーディオントグルに `aria-expanded`。ガチャボタンはローディング中に `disabled` で無効化 |
| aria-live | トースト（`#toast`）と結果エリア（`#resultArea`）に `aria-live="polite"` を付与し、動的更新をスクリーンリーダーへ通知 |
| タグボタン | 3値ステート（未設定/必ず含む/除外）に `aria-label` でテキスト状態を付与（`aria-pressed` は2値専用のため不使用） |
| ドロップダウン | `role="listbox"` + `role="option"` + `aria-selected` でスクリーンリーダー対応 |
| キーボード操作 | ピン検索ドロップダウン: `ArrowDown`/`ArrowUp` でアイテム移動、`Escape` で閉じてフォーカス復帰 |
| フォーカス表示 | `:focus-visible` を使用し、マウス操作ではフォーカスリングを非表示、キーボード操作時のみ表示 |
| フォーム | ラジオボタン・チェックボックスはすべて `<label>` で囲む。`aria-label` / `role` 属性を付与 |
| タップターゲット | 44px × 44px 以上 |

### 9.5 モバイル対応

| 項目 | 要件・実装 |
|-----|------|
| 基準幅 | 375px（モバイルファースト）。タブレット・PC は max-width: 640px でセンタリング |
| 数値入力 | `inputmode="numeric"` を指定しテンキーを表示させる |
| スクロール軽減 | PFCプリセットのタップ一発設定をメインフローとし、個別数値入力は「詳細設定」として折りたたむ |
| 結果スクロール | ガチャ後に結果エリアへ `scrollIntoView({ behavior: 'smooth', block: 'nearest' })` で自動スクロール（`block: 'nearest'` によりモバイルアドレスバーの誤動作を防ぐ） |
| キーボード回避 | 未実装 |

### 9.6 ローカル開発

`file://` プロトコルでは `fetch()` が CORS エラーになるため、ローカル HTTP サーバーを使用する。

```bash
# Python（追加インストール不要）
cd torikizoku-gacha
python -m http.server 8000
# → http://localhost:8000 をブラウザで開く

# Node.js がある場合
npx serve .
```

### 9.7 保守性

| 項目 | 要件 |
|-----|------|
| データバリデーション | `menu.json` のスキーマを JSON Schema で定義し、更新後に自動バリデーションをかける |
| 更新手順 | README にメニュー改定時のチェックリストを整備する（公式PDF確認 → JSON更新 → バリデーション → push → デプロイ確認） |
| コード設計 | ガチャロジック・スコアリングを IIFE モジュール単位で分離し、将来の React + TypeScript 移行に備える |
| データ分離 | グランドメニュー（`menu.json`）と期間限定（`menu_limited.json`）を分離し、改定影響範囲を最小化する |

### 9.8 ホスティング要件

| 項目 | 要件 |
|-----|------|
| 採用サービス | GitHub Pages（無料・静的サイト向け） |
| デプロイ | `main` ブランチへの push を契機に **GitHub Actions** で `gh-pages` ブランチへ自動デプロイする |
| fetchパス | `menu.json` の fetch パスはスクリプトからの相対パスで記述し、サブディレクトリ配信でもパスがずれないようにする |
| 帯域制限 | GitHub Pages の月間帯域上限 100GB。静的サイトのトラフィックでは現実的に問題にならない |

### 9.9 ライセンス

| 項目 | 要件 |
|-----|------|
| コードライセンス | **MIT License** を適用し、リポジトリに `LICENSE` ファイルを追加する |
| データの権利表記 | メニュー・栄養成分データの著作権は株式会社エターナルホスピタリティグループに帰属する旨を README およびサイトフッターに明記する |
| 非公式表記 | 鳥貴族公式・株式会社エターナルホスピタリティグループとは無関係である旨をフッターに明記する |

---

## 10. TODO / 今後の検討事項

| # | 項目 | 状態 | 内容 |
|---|-----|------|------|
| 1 | `menu.json` の全件入力 | **完了** | グランドメニュー全品を入力済み |
| 2 | `menu_limited.json` の初期データ入力 | **完了** | 期間限定21品を入力済み（2026年4月版） |
| 3 | テイクアウト品の扱い | **確定** | 対象外。イートインのみ |
| 4 | 栄養成分不明品の扱い | **確定** | ガチャプールから除外し、結果画面に注記 |
| 5 | シェア機能 | **完了** | X（Twitter）投稿 + クリップボードコピー |
| 6 | PFCバランス設定 | **完了** | プリセット5種（バランス・筋肉増量・体脂肪減少・糖質制限・カスタム） |
| 7 | 期間限定メニュー機能 | **一時停止中** | UI・ロジックともにコメントアウト済み。復活時はコード内コメントを参照 |
| 8 | 結果表示の各メニューにPFC・塩分を追加 | **完了** | 2段レイアウト（上段: 名前＋金額 / 下段: タグ＋PFC・塩分） |
| 9 | プロフィールからPFC自動計算 | **完了** | §3.3.6参照。身長・体重・年齢・性別・活動量からTDEEを算出し目標値を自動入力（折りたたみフォーム） |
| 10 | Web Worker 移行 | 将来検討 | 試行回数増加時にメインスレッドブロック解消 |
| 11 | Service Worker / オフライン対応 | 将来検討 | 居酒屋での不安定電波対応 |
| 12 | JSON Schema バリデーション | 将来検討 | メニューデータ更新時の品質担保 |

---

## 11. 参考リンク

- サイゼリヤメニューガチャ（本家インスパイア元）: https://wxcollector.cloudfree.jp/saizeriya/
- 鳥貴族公式メニュー: https://torikizoku.co.jp/menu/
- 鳥貴族 栄養成分・アレルギー情報: https://torikizoku.co.jp/menu/（PDFリンクあり）
